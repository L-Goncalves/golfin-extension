import { useEffect } from "react"
import "./domainList.scss"
import { useStorage } from "@plasmohq/storage/hook"


export const DomainList = () => {
   

   
 

  return (
    <div className="list">
      {/* {domains.map((domain) => {
        return (
          <li>
            {domain}
            <button data-domain="${item}" className="delete-btn">
              X
            </button>
          </li>
        )
      })} */}
    </div>
  )
}
